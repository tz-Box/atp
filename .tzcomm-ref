# ATP 的 CI 默认取哪一版 TzComm（协同轨：pin + 定时浮动 的 pin 半边）
#
# 为什么钉住：日常 PR 的红绿只应反映 ATP 自己的改动。浮动跟随会让别人的提交
# 把你的 CI 搞红，红了还要先判断"是我的锅还是 tzcomm 的锅"。
# 为什么还要定时浮动（另一半）：钉死的失败模式是「tzcomm 的改动对 ATP 完全隐形」
# ——2026-09-03 的生产事故正是它的极端版本（daemon 被钉在 8/23，11 天没人 bump，
# 与客户端库版本错配导致数据面完全不通）。故 ci.yml 另有每日定时任务跟随最新分支，
# 集成漂移当天可见，且不干扰日常 PR 的红绿。
#
# 怎么 bump：改这一行的 sha，走正常 PR（像 lockfile 一样可评审、可回溯）。
# 通常在定时任务红了并确认新版无害之后，或需要 tzcomm 新特性时。
#
# 当前值取自 2026-09-04 实际验证通过的那一版（本机 editable 安装的即是它）。
# 注意：TzComm 的默认分支是 master，但 dev 领先 master 17 个提交，
# 且 D13「取消端口池」这个关键改动只在 dev 上——故钉的是 dev 上的 commit。
ebd0361fdef37dc2379c0472a76a284949cfc4d3
