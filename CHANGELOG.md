# 更新记录

本文件记录 ATP（autotest platform）每个发行版改了什么，供**确认线上跑的是哪一版、以及那一版带来了什么**。

## 怎么读

- **版本号**（`0.2.0`）= 发行版，人工提；规则：功能批次提次版本号（`0.x.0`），纯修复提修订号（`0.x.y`）。
- **契约版本**（`contract: 1.7`）= 已实现到哪一版《PatrolBox 通信与接口总契约》，**与版本号是两回事**：
  前者答「我和其他三个平台对得上哪一版」，后者答「我是哪个发行版」。
- **`build.commit`** = 在跑的究竟是哪份代码。**认这个，不认版本号** ——
  两次发行之间的所有提交共用同一个版本号，而服务可能跑着旧进程。

```bash
curl -s http://127.0.0.1:2335/atp/health | python3 -m json.tool
```

> **为什么专门加 `build` 段**：2026-09-12 真实踩过 —— 常驻服务跑着五天前的代码，
> 而 health 照报 `version: 0.1.0`、`contract: 1.7`、`ok: true`，**全绿**。
> 版本号是写死的常量，它在新旧代码上长得完全一样：
> **「我不知道线上是哪一版」与「线上是对的」由同一组输出表达。**
> 查了数轮才发现。`commit` 让这件事变得可见。

---

## 0.2.0 — 2026-09-12

首个有更新记录的版本。此前版本号自 `0.1.0` 起从未变动，故本条汇总 `0.1.0` 之后的全部改动。

### 新增

- **A11 场景级期望结果**：被测仓可声明「本场景预期失败」，实际失败即**符合预期**，
  不再拖红整次评测。此前一条 `expect: fail` 的体检用例会让四个下游同时被喂错误事实
  （GitHub 打叉 / PMS 推飞书告警真人 / Hub 概览失败数虚增 / 交付物冻结不了）。
- **A11 批 0 · 判据层**：`Score` 从 `{指标, 通过与否}` 扩展为带判据清单，
  `passed` 改为**从判据派生**（判据非空且全部通过）。
  连带堵住一个洞：**零判据不再默认判过** —— 四个判分器「数据没到 / 真值缺失 / 样本不足」
  的早退分支改走 `not_run`（声明的判据全部在场、状态 `none`、恒不通过）。
  「没法评」与「判据没过」从此在报文里分得开：前者查数据源，后者查算法。
- **A11 批 1 · `metrics.testcases_detail`**：判据级明细出线（加法式可选段，既有字段一字未动）。
  Hub v0.13.4 已接（只列偏离项、`not_run` 黄不红）。
- **清单项级 `metrics` 覆盖**：判据级期望可只挂在复用同一场景文件的某一个清单项上
  （`cicd_test_slam` 的 `full` 与 `degraded` 共用场景文件，此前表达不了）。
- **`/atp/health` 增 `build` 段**：git commit / 分支 / 启动时工作区是否有未提交改动 / 启动时刻。
- **D2 job 级超时**：闸门放在开环与闭环唯一共用的每帧触点上，一处覆盖两条通路；
  **暂停期间不计入超时**（否则「挂断点看现场」会被框架杀掉，那条承诺就废了）。
- **`/atp/health` 判据升级为真回环**：此前只探 daemon 可达，**通信断了照报健康**。
- **health 回报 `contract` 版本**（总契约 v1.7-R12）。
- **算法工程师教程八篇**（`docs/00`–`docs/07`）：按「考谁 / 怎么考 / 考什么 / 算不算过」
  四问递进，所有示例为真实运行输出。

### 修复

- **`vs_baseline` 方向写死「越小越好」**（**潜伏缺陷，未在生产数据上发生**）。
  库内 7 个在用指标有 3 个越大越好。改为**方向由被测仓声明，缺省不猜**；
  并把 `same` 拆开 —— 「确实没变化」「有涨有跌」「缺声明」「无可比指标」此前挤在一个值里。
  > **性质更正（2026-09-12）**：初次上报时记为「已发生」，并以 `survived 0.98→0.40` 为证。
  > 该值**不可能出现**（`survived` 只取 1.0/0.0，那个数是举例时编的）。
  > 进一步核查：`ctrl.invp` 三个「越大越好」指标与 `survived` 强耦合
  > （判倒阈值与直立计数阈值是同一个，一超限即终局），故**在任何通过的评测里恒为常数**；
  > 不通过时 `passed` 翻转分支优先、旧代码本就报对。两个在用插件加起来，
  > **旧的写死方向在所有真实可能出现的数据上都是对的**。
  > `nav2d` / `manip.force` 有独立变动的「越大越好」指标，一旦启用即会咬 —— 故修复仍属必要。
- **声明了却零结果的场景会从报文里静默消失** —— met/unmet 都不加一，结论照报成功。
  改为**以声明的场景清单枚举，不以观测到的结果枚举**。
- **`summary` 把无指标的失败标成「数据流验证」** —— 看报告的人以为它没打分。
- **滚动基线按仓隔离**，并修正「逐指标持平」被归为「变好」。
- **补契约 §8 缺失的传感器校验**：此前只校验 body 的声明，不校验数据的事实；
  仓内 3 个用非空 body 的场景 3 个都不一致。
- **本机通路的评测也入库** —— console 此前只显示一半。
- **补声明 PyYAML / msgpack** —— CI 首跑即抓到（代码 import 了但 pyproject 没声明）。
- **契约测试改读入库副本**，消除「CI 上全部 skip」的自我讽刺。
- **`commcheck` 稳定假 FAIL**：订阅队列深度 + 无节流突发 + 固定 sleep 预热竞态。

### 内部

- 版本号收敛到单一来源。此前 `pyproject.toml` / `__init__.__version__` /
  `http.py` 的 FastAPI `version=` 各存一份 —— **三份会漂，而漂了不会报错**，
  因为三处都长得像个合法版本号。
